# control-plane

Core API + metadata DB. Source of truth for accounts, apps, deployments, and every
provisioned resource. Orchestrates the deploy-agent and the provisioners.

**Phase 0.** Not yet implemented — scaffolding placeholder.
