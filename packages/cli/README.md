# cli

`supersonic deploy` — the pro speed lane and a distribution vector (`npx supersonic deploy`).
Pushes a local folder to the control-plane; streams the deploy log.

**Phase 5.** Not yet implemented — scaffolding placeholder.
