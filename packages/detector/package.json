{
  "name": "@bay/detector",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "detect": "tsx src/index.ts",
    "test": "node --import tsx --test 'test/**/*.test.ts'"
  },
  "devDependencies": {
    "tsx": "^4.19.1",
    "typescript": "^5.6.3",
    "@types/node": "^22.7.5"
  },
  "description": "Deterministic stack detection and build planning: reads a repository and works out how to build and run it."
}
